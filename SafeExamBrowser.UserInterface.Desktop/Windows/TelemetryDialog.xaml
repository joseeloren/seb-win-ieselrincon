<Window x:Class="SafeExamBrowser.UserInterface.Desktop.Windows.TelemetryDialog" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SafeExamBrowser.UserInterface.Desktop"
        mc:Ignorable="d" Height="250" Width="450" ResizeMode="NoResize" Topmost="True" Title="El Rincón Seguro - Identificación">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Templates/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid FocusManager.FocusedElement="{Binding ElementName=StudentName}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <fa:ImageAwesome Grid.Column="0" Foreground="LightGray" Icon="User" Margin="25" Width="50" />
                <Grid Grid.Column="1" Margin="0,20,25,25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Por favor, introduce tu Nombre completo:" Margin="0,0,0,5" TextWrapping="WrapWithOverflow" />
                    <TextBox Grid.Row="1" x:Name="StudentName" Height="25" VerticalContentAlignment="Center" Margin="0,0,0,15" />
                    
                    <TextBlock Grid.Row="2" Text="Código del Examen:" Margin="0,0,0,5" TextWrapping="WrapWithOverflow" />
                    <TextBox Grid.Row="3" x:Name="ExamCode" Height="25" VerticalContentAlignment="Center" />
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="1" Height="50" Background="{StaticResource BackgroundBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <WrapPanel Orientation="Horizontal" Margin="25,0" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="ConfirmButton" Cursor="Hand" Margin="10,0" Padding="10,5" MinWidth="75" Content="Conectar" />
            </WrapPanel>
        </Grid>
    </Grid>
</Window>
